{
  "framework": "jekyll",
  "buildCommand": "npm install -g vercel@34.3.0 && bundle config set force_ruby_platform true && bundle install && NOKOGIRI_USE_SYSTEM_LIBRARIES=true bundle exec jekyll build --trace",
  "outputDirectory": "_site",
  "env": {
    "BUNDLE_FORCE_RUBY_PLATFORM": "true",
    "NOKOGIRI_USE_SYSTEM_LIBRARIES": "true"
  }
}
